<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5">

    <!-- Custom Toolbar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="#1976D2"
        android:gravity="center_vertical"
        android:paddingStart="4dp" android:paddingEnd="4dp"
        android:orientation="horizontal">

        <ImageButton android:id="@+id/backBtn"
            android:layout_width="40dp" android:layout_height="40dp"
            android:src="@android:drawable/ic_media_previous"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Back"/>

        <ImageButton android:id="@+id/forwardBtn"
            android:layout_width="40dp" android:layout_height="40dp"
            android:src="@android:drawable/ic_media_next"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Forward"/>

        <ImageButton android:id="@+id/refreshBtn"
            android:layout_width="40dp" android:layout_height="40dp"
            android:src="@android:drawable/ic_menu_rotate"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Refresh"/>

        <ImageButton android:id="@+id/homeBtn"
            android:layout_width="40dp" android:layout_height="40dp"
            android:src="@android:drawable/ic_menu_myplaces"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Home"/>

        <EditText android:id="@+id/urlBar"
            android:layout_width="0dp" android:layout_height="36dp"
            android:layout_weight="1" android:background="#FFFFFF"
            android:hint="Enter URL" android:textSize="13sp" android:paddingStart="8dp"
            android:inputType="textUri" android:imeOptions="actionGo"
            android:singleLine="true"/>

        <Button android:id="@+id/goBtn"
            android:layout_width="wrap_content" android:layout_height="36dp"
            android:text="Go" android:backgroundTint="#0D47A1" android:textColor="#FFFFFF"
            android:layout_marginStart="4dp"/>
    </LinearLayout>

    <!-- Progress Bar for loading -->
    <ProgressBar
        android:id="@+id/loadProgressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:max="100"
        android:progress="0"
        android:progressTint="#FF5722"
        android:visibility="gone"/>

    <!-- Shortcut Buttons Row -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#EEEEEE"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="6dp">

            <Button android:id="@+id/btnGoogle"
                android:layout_width="wrap_content" android:layout_height="34dp"
                android:text="Google" android:textSize="12sp"
                android:layout_marginEnd="6dp" android:backgroundTint="#4285F4"
                android:textColor="#FFFFFF"/>
            <Button android:id="@+id/btnYouTube"
                android:layout_width="wrap_content" android:layout_height="34dp"
                android:text="YouTube" android:textSize="12sp"
                android:layout_marginEnd="6dp" android:backgroundTint="#FF0000"
                android:textColor="#FFFFFF"/>
            <Button android:id="@+id/btnWikipedia"
                android:layout_width="wrap_content" android:layout_height="34dp"
                android:text="Wikipedia" android:textSize="12sp"
                android:layout_marginEnd="6dp" android:backgroundTint="#333333"
                android:textColor="#FFFFFF"/>
            <Button android:id="@+id/btnKhan"
                android:layout_width="wrap_content" android:layout_height="34dp"
                android:text="Khan Academy" android:textSize="12sp"
                android:layout_marginEnd="6dp" android:backgroundTint="#14BF96"
                android:textColor="#FFFFFF"/>
            <Button android:id="@+id/btnUniversity"
                android:layout_width="wrap_content" android:layout_height="34dp"
                android:text="University" android:textSize="12sp"
                android:backgroundTint="#8E24AA" android:textColor="#FFFFFF"/>
        </LinearLayout>
    </HorizontalScrollView>

    <!-- WebView -->
    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

</LinearLayout>